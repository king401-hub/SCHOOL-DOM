<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the kiosk's own dark theme background (see AppColors /
         KioskHomeScreen) so the native launch screen doesn't flash white
         before the Flutter splash takes over. -->
    <color name="launch_background">#0B1220</color>
</resources>
